# Vitrina — Handoff
> آخرین آپدیت: 2026-08-03

## الان
صفحه‌ی «ایجاد کمپین همکاری در فروش» (`/marketing/campaigns/new`) پیاده‌سازی شد — دسکتاپ کامل، هنوز commit نشده:

- `CampaignNewSales.tsx` (view) + `CampaignFeeInput.tsx`, `ProductPickerDialog.tsx`, `ProductPickerItem.tsx` (کامپوننت‌های محلی)
- تمام فیلدها با `Field.Root`/`Field.Label`/`Field.RequiredIndicator`/`Field.HelperText` (نه Flex/Text دستی) — ستاره اجباری بعد از لیبل (چپ در RTL)
- «پیش نمایش مالی»: سه Stat با توکن‌های `brand.bg`/`purple.bg`/`blue.bg` (دو تای آخر تازه به `tokens.ts` اضافه شدن، چون Chakra پیش‌فرض `.bg` نداره) — ترتیب طبق درخواست کاربر: قیمت محصول → سهم بازاریاب → سهم فروشنده (خلاف ترتیب اولیه‌ی Figma، عمداً)
- ریسپانسیو `<sm` کامل شد: بنر نوع کمپین ستونی، فیلد مبلغ کارمزد زیر توگل، کارت محصول انتخاب‌شده ستونی با دکمه تمام‌عرض، سه باکس پیش‌نمایش مالی زیر هم، تاریخ پایان زیر تاریخ شروع
- `ButtonFooter`: پراپ اختیاری جدید `back.hideIcon` اضافه شد (پیش‌فرض false، فقط همین صفحه ازش استفاده می‌کنه — بقیه صفحات دست‌نخورده)

## بعدی
تصمیم کاربر: **کامیت همین تغییرات** (درخواست شد، هنوز انجام نشده — با `wf-commit` ادامه بده).
بعد از کامیت: کاربر گفت هر وقت دسکتاپ تموم شد، طرح‌های ریسپانسیو/موبایل رو از Figma می‌فرسته برای تطبیق دقیق‌تر.

## باگ‌های open
- `ProductPickerDialog`/`ProductPickerItem` زیر ~375px تنگ می‌شن (متن/قیمت/SKU جمع می‌شن روی هم) — دیده شد ولی چون خارج از درخواست صریح این session بود فیکس نشد.
