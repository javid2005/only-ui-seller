# چک‌پوینت‌ها — صف کار و نقاط بازگشت

صفحهٔ زندهٔ نسخه‌ها: https://claude.ai/code/artifact/e1a63324-9e14-450c-beb5-81f11d4aaece

هر چک‌پوینت یک کامیت روی `claude/kind-hypatia-97h8qn` است و یک نسخه در صفحهٔ بالا.
برگشت به مرحلهٔ قبل: `git revert <sha>` (نگه‌داشتن تاریخچه) یا `git checkout <sha>`.

> تگ گیت ساخته نشد: اعتبارنامهٔ این سشن اجازهٔ push روی `refs/tags` را ندارد (۴۰۳).
> به‌جایش SHA مرجع است.

## انجام‌شده

| # | موضوع | SHA |
|---|-------|-----|
| ۱ | هفت مورد باز دور آخر («چاکرا اصلاح۲» که منتشر نشد) | `f631798` |
| ۲ | انتخاب نوع محصول (ساده / متنوع) + نشانهٔ بصری انتخاب | `5b744c8` |
| ۳ | ساختار شش‌مرحله‌ای + مرحلهٔ انبارداری و ارسال + مشخصات محصول | `c4ad5fd` |
| ۴ | مرحلهٔ سئو و انتشار — امتیاز واقعی + پرش به هشدار | `8d9a518` |
| ۵ | پیش‌نمایش زندهٔ محصول در ریل (و فشرده در موبایل) | `6b09db4` |
| ۶ | گالری کامل (فایل‌منیجر) | `f446740` |
| ۷ | راهنماهای مدال با متن نسخهٔ تأییدشده | `e3a595d` |
| ۸ | پاس بصری مرحلهٔ ۱ + کلیدهای hover-only | `975fdb1` |

## صف

همهٔ موارد صف اولیه انجام شد. باقی‌مانده‌ها در بخش بعد.

## زبان بصری — کجا هنوز از طرح فاصله دارد

چک‌پوینت ۳ تقسیم‌بندی و اسکلت بصری را همگرا کرد. آنچه هنوز مانده و در چک‌پوینت‌های
بعدی بسته می‌شود:

- نوار ابزار بالای صفحه (ذخیره/پیش‌نمایش) به‌صورت کارت مستقل
- انیمیشن‌های ورود بخش‌ها و جابه‌جایی بین مراحل

## منبع خواسته‌ها

سه دور بازخورد، متن کامل در `history/ChatGPT-product-editor.md`:
۱۴ مورد («چاکرا طراح») · ۱۱ مورد («چاکرا اصلاح») · ۷ مورد (دور آخر، منتشرنشده).
هر چیزی که در «چاکرا اصلاح» هست freeze است مگر موردی که صریح خواسته شده باشد.
