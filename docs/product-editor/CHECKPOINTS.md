# چک‌پوینت‌ها — صف کار و نقاط بازگشت

صفحهٔ زندهٔ نسخه‌ها: https://claude.ai/code/artifact/e1a63324-9e14-450c-beb5-81f11d4aaece

هر چک‌پوینت یک کامیت روی `claude/kind-hypatia-97h8qn` است و یک نسخه در صفحهٔ بالا.
برگشت به مرحلهٔ قبل: `git revert <sha>` (نگه‌داشتن تاریخچه) یا `git checkout <sha>`.

> تگ گیت ساخته نشد: اعتبارنامهٔ این سشن اجازهٔ push روی `refs/tags` را ندارد (۴۰۳).
> به‌جایش SHA مرجع است.

## انجام‌شده

| # | موضوع | SHA |
|---|-------|-----|
| ۱ | هفت مورد باز دور آخر («چاکرا اصلاح۲» که منتشر نشد) | `f631798` |

## صف

ترتیب بر اساس وابستگی انتخاب شده: هر مورد پیش‌نیازِ موردهای بعدی است.

| # | موضوع | چرا این ترتیب |
|---|-------|----------------|
| ۲ | انتخاب نوع محصول (ساده / متنوع) | دروازهٔ ورود است و تعیین می‌کند مرحلهٔ تنوع‌ها اصلاً دیده شود یا نه |
| ۳ | استپر شش‌مرحله‌ای + مرحلهٔ «انبارداری و ارسال» | ساختار مراحل، که بقیهٔ تب‌ها رویش سوار می‌شوند |
| ۴ | تب «سئو و انتشار» + امتیاز سئو و پرش به هشدار | یک مرحلهٔ مستقل روی استپر جدید |
| ۵ | پیش‌نمایش محصول (sticky دسکتاپ · فشرده/تمام‌صفحه موبایل) | به داده‌های همهٔ مراحل قبل نیاز دارد |
| ۶ | گالری کامل: نمای شبکه‌ای/لیستی، انتخاب گروهی، جست‌وجوی نام فایل، مدال آپلود | روی پوشه‌های چک‌پوینت ۱ سوار می‌شود |
| ۷ | راهنماهای مدال (گرید رنگی + باکس ویدئو) | سراسری است و آخر از همه یک‌دست می‌شود |

## منبع خواسته‌ها

سه دور بازخورد، متن کامل در `history/ChatGPT-product-editor.md`:
۱۴ مورد («چاکرا طراح») · ۱۱ مورد («چاکرا اصلاح») · ۷ مورد (دور آخر، منتشرنشده).
هر چیزی که در «چاکرا اصلاح» هست freeze است مگر موردی که صریح خواسته شده باشد.
